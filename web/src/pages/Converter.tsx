import { Card, CardHeader, Text } from '@ui5/webcomponents-react'

export default function Converter() {
  return (
    <Card header={<CardHeader titleText="XML ↔ JSON Converter" />}>
      <div style={{ padding: '1rem' }}>
        <Text>Converter — coming in step 3.</Text>
      </div>
    </Card>
  )
}
