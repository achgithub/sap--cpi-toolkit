# IAS credentials — empty for local (auth bypass is active).
# Populated via BTP service binding or kubectl create secret in the kyma overlay.
apiVersion: v1
kind: Secret
metadata:
  name: ias
stringData:
  IAS_CLIENT_ID: ""
  IAS_CLIENT_SECRET: ""
  IAS_TENANT_URL: ""
