apiVersion: v1
kind: Service
metadata:
  name: sftp-adapter
spec:
  # LoadBalancer exposes port 2222 on localhost (matches docker-compose mapping)
  type: LoadBalancer
  selector:
    app: sftp-adapter
  ports:
    - port: 2222
      targetPort: 22
